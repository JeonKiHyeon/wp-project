.form-group
  label(for="title") Events
  input.form-control(type="text", name="title", placeholder="What's your events?", value=event.title)
.form-group
  label(for="event") Contents
  textarea.form-control(name="content", rows="5")= event.content
.form-group
  label(for="tags") Tags
  input.form-control(type="text", name="tags", placeholder="tags separated by spaces", value=event.tags ? event.tags.join(" ") : "")
